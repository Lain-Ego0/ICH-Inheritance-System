server:
  tomcat: #tomact 服务器的配置
    relaxed-query-chars: '|,{,},[,]'  #进行get请求时参数包含[,{,},]视为正常
  port: 7245 # 端口

#数据库名：mysql，用户名root，密码123456
spring:
  #配置邮箱信息
  mail:
    username: 806105498@qq.com
    password: jhtrzsovfmaebdgg
    host: smtp.qq.com
    properties:
      mail:
        smtp:
          ssl:
            enable: true
  servlet:
    multipart: #文件的上传配置
      max-request-size: 10000MB #请求的最大报文长度
      max-file-size: 10024MB #上传的文件最大限制
  datasource: #数据库的配置
    username: root #数据库的账号
    password: 23456789 #数据库的密码
    url: jdbc:mysql://localhost:3306/feiyixitong?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver #显式指定MySQL驱动（关键！）
    # 数据库连接池配置（解决连接超时/不稳定问题）
    hikari:
      maximum-pool-size: 10  # 最大连接数
      minimum-idle: 5        # 最小空闲连接
      idle-timeout: 300000   # 连接空闲超时（5分钟）
      connection-timeout: 20000  # 连接超时（20秒）
  jackson: # json序列化配置
    property-naming-strategy: UPPER_CAMEL_CASE #大写开头命名风格
    date-format: yyyy-MM-dd HH:mm:ss #时间格式
    time-zone: Asia/Shanghai #时区

mybatis-plus: #MyBatis-Plus配置
  mapper-locations: mapper/*.xml #mapper文件位置
  global-config: #全局配置
    db-config: #数据库配置
      table-prefix:   #表前缀（无则留空）
      id-type: auto #主键自增（根据你的表结构调整）
  configuration:
    # log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启SQL日志（调试时可打开）
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl
    map-underscore-to-camel-case: false #是否下划线转驼峰
  type-aliases-package: com.example.web.entity  #实体类包位置

os:
  code: E0647CDEB655452CBB629700B99ECA95